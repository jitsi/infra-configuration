# Managed by Ansible - do not edit manually
{% for registry, creds in docker_ecr_credentials.items() %}
[{{ registry }}]
aws_access_key_id = {{ creds.aws_access_key_id }}
aws_secret_access_key = {{ creds.aws_secret_access_key }}

{% endfor %}
