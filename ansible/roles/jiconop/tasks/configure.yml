---
# eventually build a config file for jiconop
- name: Install jiconop configuration file
  ansible.builtin.template:
    mode: 0644
    src: "config.j2"
    dest: "/etc/jitsi/jiconop/config"
  notify: Restart jiconop
