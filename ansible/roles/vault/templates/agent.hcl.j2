vault {
  address = "{{ vault_agent_address }}"
  retry {
    num_retries = 5
  }
}

auto_auth {
  method "{{ vault_agent_auth_method }}" {
    config = {{ vault_agent_auth_config }}
  }

  sink "file" {
    config = {
      path = "/home/vault/.vault-token"
    }
  }
}
