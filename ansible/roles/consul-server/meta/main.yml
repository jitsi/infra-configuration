---
galaxy_info:
  role_name: consul_server
  author: Aaron van Meerten
  description: install and configure consul server
  license: Apache License Version 2.0
  min_ansible_version: '6.6.0'
  platforms:
    - name: Ubuntu
      releases:
        - focal
        - jammy
dependencies:
  - { role: consul-install, when: consul_install_flag }
